import styled from "styled-components";

const Container = styled.div`

`

const RecordsFilter = (props: any) => {
  return (
    <Container className={props.className}>
      
    </Container>
  );
}

export default RecordsFilter;